"""AutoGluon ML layer for AI Signal Pro.

This package is intentionally independent from the existing TypeScript signal
engine. AutoGluon is imported lazily so the main app can run without the
optional training dependency installed.
"""

