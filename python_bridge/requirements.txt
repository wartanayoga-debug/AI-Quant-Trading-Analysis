fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.7.0
numpy>=1.26.0,<2.2
pandas>=2.2.0
scipy>=1.13.0
hmmlearn>=0.3.2
httpx>=0.27.0
torch>=2.0.0
einops>=0.8.0
huggingface_hub>=0.24.0,<1
fsspec[http]<=2025.3.0
safetensors>=0.4.0
tqdm>=4.66.0
transformers>=4.44.0,<5

# Kronos foundation model (optional)
# Enabled by default through the bundled local kronos_model package.
# First run downloads Hugging Face model weights configured by KRONOS_MODEL_ID.

# Optional but recommended for real Chronos inference.
# Install manually when the machine has enough RAM/CPU budget:
# pip install torch chronos-forecasting
